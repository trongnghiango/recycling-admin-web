import React from "react";

const s = {
  wrapper: `flex`,
};

// interface IProps {}

export const Footer: React.FC = () => {
  return (
    <div className={s.wrapper}>
      {/* control */}

      {/* steps */}
    </div>
  );
};
